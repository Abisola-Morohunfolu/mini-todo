export const lightEffectClass = 'before:absolute before:inset-0 before:h-inherit after:h-inherit before:shadow-[inset_0_1px_2px_rgba(255,255,255,0.3)] after:absolute after:inset-0 after:shadow-[0_-1px_2px_rgba(0,0,0,0.2)]'

export type Todo = {
  id: string;
  todo: string;
  isCompleted: boolean;
}